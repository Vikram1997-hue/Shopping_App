import 'package:flutter/material.dart';

class Signup extends StatelessWidget {
  //const ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("TA DA!", style: TextStyle(fontSize: 100),),
    );
  }
}
